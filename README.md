# Koa2 hello world backend

This backend uses mongo database and has mocha to test its endpoints.

For running the test:

Environment variables
  -ENV: test

Mocha options: --require babel-core/register